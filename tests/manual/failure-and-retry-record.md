# 失败与重试记录

> 当前状态：尚未执行 Day 3 规定的完整用户失败恢复测试。后端和 Worker
> 的自动化失败用例不能替代本记录；不得根据测试代码推测页面表现。

- 日期：TODO
- 失败环节：TODO
- 复现输入：TODO
- 页面提示：TODO
- 日志 Trace：TODO（必须脱敏）
- 根因：TODO
- 修复：TODO
- 再次运行结果：TODO

## 2026-08-10：真实报告深链接缺少浏览器会话

- 日期：2026-08-10
- 失败环节：合并 PR #35 后，直接打开真实报告深链接。
- 复现输入：本地课堂 `716c0995-d0a7-49ab-a9c7-a0d7dc7e36d9`；不记录课堂原文或预签名地址。
- 页面提示：只显示“报告暂时无法载入”，没有说明登录态或恢复方法。
- 日志 Trace：Next.js BFF 记录 `GET /api/classrooms/{id}/report 401`；该请求未返回业务 Trace。
- 根因：当前浏览器没有 HttpOnly 演示会话 Cookie；接口与报告数据本身正常。
- 修复：页面识别 `ApiClientError.status === 401`，提供“建立演示会话并重试”；调用同源
  `/api/session/demo` 设置 HttpOnly Cookie 后自动重载报告，不把令牌交给前端 JavaScript。
- 再次运行结果：成员 1 刷新同一深链接并点击“建立演示会话并重试”后，页面成功进入
  “编辑、预览与导出复盘报告”。后端日志依次记录 `POST /api/auth/demo 200` 与
  `GET /api/classrooms/{id}/report 200`。本次只证明 401 恢复链路与空报告载入成功，
  不代表含真实结论的报告门禁或下载文件内容已经人工验收。

## 2026-08-10：真实空报告持久化、预览与 PDF 导出

- 验收输入：上述本地课堂；数据库当时为 `0` 条分析结论，故只验证真实空状态，不构造假结论。
- 标题保存：成员 1 将标题改为“课堂复盘报告（人工验收）”，页面成功提示保存；前后端日志均
  记录 `PUT /api/classrooms/{id}/report 200`。
- 预览：标题、空结论提示和“纳入 0 条经教师确认的结论”均与数据库一致。
- PDF 导出：页面提示生成成功，下载链接可用，PDF 可打开且标题正确；后端日志记录
  `POST /api/reports/{id}/export 201`。预签名地址未写入本记录。
- 其他格式：Markdown、HTML 已通过同源 BFF 冒烟验证并返回短时地址，但本轮未逐一人工打开。
- 验收边界：本项证明真实报告空状态、持久化和 PDF 导出可用，不证明 accepted/modified
  内容纳入或 pending/rejected 排除的浏览器表现；该项须等待真实结论数据后复验。
