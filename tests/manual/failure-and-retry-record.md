# 失败与重试记录

> 当前状态：已执行一条真实视频的技术纵向链路，并记录两类真实失败与恢复；
> 非开发用户试用、同任务修改后复测和第二段远程输入仍未执行。后端和 Worker
> 的自动化失败用例不能替代本记录；不得根据测试代码推测页面表现。

- 日期：TODO
- 失败环节：TODO
- 复现输入：TODO
- 页面提示：TODO
- 日志 Trace：TODO（必须脱敏）
- 根因：TODO
- 修复：TODO
- 再次运行结果：TODO

## 2026-08-10：真实报告深链接缺少浏览器会话

- 日期：2026-08-10
- 失败环节：合并 PR #35 后，直接打开真实报告深链接。
- 复现输入：本地课堂 `716c0995-d0a7-49ab-a9c7-a0d7dc7e36d9`；不记录课堂原文或预签名地址。
- 页面提示：只显示“报告暂时无法载入”，没有说明登录态或恢复方法。
- 日志 Trace：Next.js BFF 记录 `GET /api/classrooms/{id}/report 401`；该请求未返回业务 Trace。
- 根因：当前浏览器没有 HttpOnly 演示会话 Cookie；接口与报告数据本身正常。
- 修复：页面识别 `ApiClientError.status === 401`，提供“建立演示会话并重试”；调用同源
  `/api/session/demo` 设置 HttpOnly Cookie 后自动重载报告，不把令牌交给前端 JavaScript。
- 再次运行结果：成员 1 刷新同一深链接并点击“建立演示会话并重试”后，页面成功进入
  “编辑、预览与导出复盘报告”。后端日志依次记录 `POST /api/auth/demo 200` 与
  `GET /api/classrooms/{id}/report 200`。本次只证明 401 恢复链路与空报告载入成功，
  不代表含真实结论的报告门禁或下载文件内容已经人工验收。

## 2026-08-10：真实空报告持久化、预览与 PDF 导出

- 验收输入：上述本地课堂；数据库当时为 `0` 条分析结论，故只验证真实空状态，不构造假结论。
- 标题保存：成员 1 将标题改为“课堂复盘报告（人工验收）”，页面成功提示保存；前后端日志均
  记录 `PUT /api/classrooms/{id}/report 200`。
- 预览：标题、空结论提示和“纳入 0 条经教师确认的结论”均与数据库一致。
- PDF 导出：页面提示生成成功，下载链接可用，PDF 可打开且标题正确；后端日志记录
  `POST /api/reports/{id}/export 201`。预签名地址未写入本记录。
- 其他格式：Markdown、HTML 已通过同源 BFF 冒烟验证并返回短时地址，但本轮未逐一人工打开。
- 验收边界：本项证明真实报告空状态、持久化和 PDF 导出可用，不证明 accepted/modified
  内容纳入或 pending/rejected 排除的浏览器表现；该项须等待真实结论数据后复验。

## 2026-08-10：真实视频到报告的技术纵向验收与失败恢复

- 验收输入：一段此前已获授权用于本项目的真实课堂视频；不提交视频、完整逐字稿、
  预签名地址、模型推理或任何密钥。
- 旧任务隔离：历史任务 `adb184e8-3a84-42c8-9d78-f8881b57d5ae` 的分析契约已不符合
  当前 Schema。Worker 回收过期租约后，后端将其安全隔离为 `failed/transcribe`，错误码
  `VALIDATION_ERROR`，要求重新创建任务，没有继续使用不兼容数据。
- 旧对象失效：使用旧资产创建的新任务 `a22c03ac-d61b-4c36-8410-d5fa1f1bec26` 在下载阶段
  失败。签名 GET 的字节范围请求实测返回 `404`，任务以 `UPSTREAM_UNAVAILABLE` /
  `OBJECT_DOWNLOAD_FAILED` 结束。该现象与七天保留期一致，但本记录只能证明对象不存在，
  不把具体删除执行方写成已确认事实。
- 恢复操作：从本机获授权原文件重新执行 `presign → PUT 200 → HEAD complete 200`，新资产
  `549b829a-4e32-4a31-92f9-667632938169` 经后端核验为 `uploaded`；随后以当前已确认分析
  契约创建任务 `f724f190-17c0-4b1e-ba03-32b1231effcf`。
- Worker 结果：任务完成 B2 限时下载、FFmpeg、Whisper 和 PostgreSQL 写回，生成 1 条真实
  时间戳逐字稿片段，并成功交接 Agent。
- Agent 结果：本地 Ollama `qwen3.5:4b` 使用 `analysis-v2` Prompt 和 `common` Skill，按同一
  Trace `9d8f9b5528c3418eb56bf9b2c8f94469` 写入事实、判断、建议各 1 条；每条均有 1 个指向
  真实逐字稿片段且含起止时间的证据引用，任务最终为 `succeeded/analyze/1.0`。
- 复核门禁：通过教师公开业务 API 分别提交接受、修改、驳回，三个请求均返回 `201`；
  修改内容与备注进入复核历史。报告精确包含 accepted 与 modified 两条，使用教师改写文本，
  模型原判断与 rejected 内容均未进入正文。
- 导出结果：Markdown、HTML、PDF 均返回 `201` 并从对象存储成功下载；字节数分别为
  `378`、`553`、`2936`，三种文件均验证标题，PDF 另经文本解析验证。
- 页面与审计：真实任务页 HTTP 为 `200`；数据库审计存在资产上传请求/核验、任务创建、
  逐字稿替换、报告创建/更新和报告导出事件。敏感值和课堂正文未写入本记录。
- 发布门禁：首次运行因 Codex 沙箱无权写入 Windows 用户临时目录而出现 49 个 fixture
  `PermissionError`，不是断言失败；将 `TEMP/TMP` 指向可写工作区并禁用 pytest 缓存后，
  正式 `verify.ps1` 通过：Python `267 passed, 11 skipped`、Ruff、前端测试、TypeScript
  类型检查与 Next.js 生产构建全部成功。临时目录随后删除。
- 验收边界：这是成员 1 监督下使用演示教师账号和公开业务 API 完成的真实数据技术 E2E，
  证明核心服务链与报告门禁，不冒充非开发教师独立点击完成的可用性测试；第二段远程输入、
  真实翻译、课件证据和同任务修改后复测仍待完成。
