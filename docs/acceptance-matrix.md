# 验收矩阵

> 状态更新：2026-07-30，基于 `main@02d8e41` 与待合并 PR #31 `b8ea08a`；
> PR #31 的 scaffold、backend-check、frontend-check 全部通过。
> `已通过` 只用于证据满足本行完整验收条件的项目；`局部通过` 不能计入 M1 完成。

| 要求 | 主责 | 复核 | 当前状态 | 已有证据与剩余缺口 |
|---|---|---|---|---|
| 对话追问与分析契约 | 成员 1 | 成员 5 | 局部通过 | 前端交互和回归测试存在；PR #26 已让冻结契约进入 Agent 领取响应和编排，本地真实模型已通过合成证据调用，但未完成真实视频全链路 |
| Backblaze B2 私有存储、预签名 URL 与归属 | 成员 3 | 成员 4 | 局部通过 | 浏览器和 Worker 完成 `presign → PUT → HEAD complete → task → limited download`；下载核对大小与 ETag，仍需第二输入、重启恢复与部署证据 |
| S3 Provider 可替换边界 | 成员 3 | 成员 5 | 已通过自动化 | `backend/app/services/storage.py` 与存储测试；业务路由通过 Provider 接口访问存储 |
| Bucket 最小权限、7 天保留与删除 | 成员 3 | 成员 4、5 | 局部通过 | 私有上传、核验和删除接口已实现；仍需保存最小权限、生命周期及删除审计的脱敏部署证据 |
| 视频到时间戳逐字稿 | 成员 4 | 成员 3 | 局部通过 | 两段真实视频本地 Worker 测试通过；PR #20 证明一段浏览器 B2 上传对象可下载、转写并写回 PostgreSQL；仍需第二段远程输入及前端读取复测 |
| 双语逐字稿可编辑 | 成员 2 | 成员 4 | 局部通过 | 后端逐字稿读取/编辑接口和前端双语演示结构存在；真实翻译和页面接线未完成 |
| 结论与证据联动 | 成员 2 | 成员 4 | 局部通过 | 演示播放器、时间轴和证据卡可联动；真实证据索引和 API 数据未接入 |
| 事实/判断/建议 Schema | 成员 5 | 成员 3 | 已通过自动化 | 冻结 Schema、Agent 结构化输出及校验测试 |
| 接受、修改、驳回与历史 | 成员 2、3 | 成员 5 | 局部通过 | 后端写入/历史 API、owner 隔离和 PostgreSQL 集成测试已在 PR #31 CI 复验；前端仍只改页面状态 |
| 仅已复核内容进入报告 | 成员 5 | 成员 3 | 局部通过 | Agent 组合器及后端服务端持久化过滤已在 PostgreSQL CI 通过；前端仍未接入真实结论与报告 |
| 报告编辑、预览、导出 | 成员 1 | 成员 5 | 局部通过 | 编辑、预览和浏览器打印/PDF 已验证，后端保存/读取 API 已实现；当前页面仍走 `/reports/demo` 与 `sessionStorage` |
| 第二输入重新分析 | 成员 4 | 成员 5 | 局部通过 | 本地 Worker 样例证明两段视频逐字稿不同；尚未完成网页第二输入全链路 |
| 两账号隔离 | 成员 3 | 成员 5 | 已通过自动化 | PostgreSQL 测试覆盖跨账号课程、课堂、任务、资源、结论和报告访问；仍建议补部署网页复测 |
| 完整流程与失败场景 | 成员 5 | 成员 1 | 未通过 | PR #26 已合并 Worker→Agent 交接、租约续期、结论写回和安全失败自动化；本地 Ollama `qwen3.5:4b` 已通过真实 HTTP 与完整 Agent Schema 合成证据验证，但尚未使用真实视频逐字稿，上传到报告 E2E、失败恢复人工记录与同任务复测仍缺失 |
| UI Baseline v1 与流程入口 | 成员 1 | 成员 2、5 | 已通过 | 首页、课堂创建、复盘目标与分析契约可操作；Mock 边界醒目；前端测试、类型检查和构建通过 |
| UI 可读性与适配 | 成员 2 | 成员 1、5 | 局部通过 | 静态回归、无脚本可读性和减少动态效果已验证；真实用户试用延期 |
| 证据工作台继承共享视觉令牌 | 成员 2 | 成员 1 | 已通过 | 成员 2 组件已合并并由成员 1 修复集成回归；共享视觉基准保留 |
| Trace 持久化、前端可见与脱敏 | 成员 5 | 成员 3 | 局部通过 | PR #31 中 Agent 运行器默认写入权限收敛的 JSONL，任务面板展示 `trace_id`，敏感正文与错误详情脱敏测试通过；待复审合并和真实任务日志抽查 |
| 前后端、Worker、Agent 统一启动 | 成员 5 | 成员 2、3、4 | 局部通过 | Windows PowerShell 语法、跨平台静态服务覆盖、令牌不入 CLI 和缺 `.env` fail-closed 已验证；完整基础设施实机启动与恢复尚待执行 |
