# 提交前评分自检

> 当前状态：2026-07-30 过程性自检，不是最终得分。里程碑 M1 尚未通过，
> 因此现在不得按满分材料提交。逐项事实见 `current-progress.md` 和
> `acceptance-matrix.md`。

## 实际交付内容（60 分）

- 共同任务、方向成果与用户价值：20 分。
- 核心流程与产出：20 分。
- 真实实现与可用性：20 分。

当前风险：

- 上传、B2 下载、媒体转写和 PostgreSQL 逐字稿写回已形成单输入真实纵向切片，
  但尚未到达真实报告。
- PR #26 的 Agent 运行器已合入并通过离线自动化；本地 Ollama `qwen3.5:4b`
  已完成真实 HTTP/结构化 Schema 调用，但输入仍是合成证据，不能替代真实视频验收。
- 教师复核历史和报告持久化后端代码/数据库测试已出现，统一启动与发布门禁代码也已完成；
  当前仍缺 PostgreSQL 实机复验、前端真实接线、完整 E2E 和正式部署证据。
- 因此“核心流程与产出”和“真实实现与可用性”暂不具备最终评分证据。

## 小组报告（30 分）

- 需求与产品设计：10 分。
- 技术实现与实际完成范围：10 分。
- 验证复盘与 AI 协作：10 分。

当前风险：

- `reports/group-report.md` 已从纯骨架更新为过程稿，但仍需各成员补充本人实现和证据。
- 真实用户试用与同任务复测延期，不能编造反馈或验证结论。
- AI 协作与跨成员修复必须保留原作者和协作归属。

## 个人贡献（10 分）

每位成员说明负责内容、可核对成果和协作贡献。

当前要求：贡献文件只记录可由提交、PR、测试或人工记录核对的事实；成员 1
协作完成的跨模块修复不得改写成其他成员已独立完成，其他成员的原始实现也不得
归入成员 1 的独立成果。

## 提交前仍须清零的门禁

- [x] Worker 能处理浏览器上传到 B2 的真实视频（PR #20；单输入真实验收）。
- [ ] 真实逐字稿、证据、Agent 结论、教师复核和报告形成完整链路。
- [ ] 教师复核状态、修改内容和历史写入后端（代码及数据库测试已有，本轮环境未执行，前端未接线）。
- [ ] 报告可持久化，并只读取已接受或已修改内容（代码及测试已有，本轮数据库/网页 E2E 未执行）。
- [ ] 第二段不同视频通过网页全链路复测。
- [ ] 一次成功流程、一次常见失败恢复和一次同任务修改后复测有记录。
- [ ] 一键启动、部署地址、源码说明和全量回归可由其他成员复现（本机发布门禁已通过；完整基础设施、浏览器和他人复现仍缺）。
