# 提交前评分自检

> 当前状态：2026-08-10 过程性自检，不是最终得分。单输入真实数据技术 E2E 已通过，
> 里程碑 M1 仍因第二输入、同任务复测和非开发教师试用尚未全部通过，
> 因此现在不得按满分材料提交。逐项事实见 `current-progress.md` 和
> `acceptance-matrix.md`。

## 实际交付内容（60 分）

- 共同任务、方向成果与用户价值：20 分。
- 核心流程与产出：20 分。
- 真实实现与可用性：20 分。

当前风险：

- 上传、B2 下载、媒体转写、PostgreSQL 逐字稿、本地模型结论、教师复核和三格式报告
  已形成单输入真实数据技术 E2E。
- PR #26 的 Agent 运行器已合入并通过离线自动化；本地 Ollama `qwen3.5:4b`
  已以真实 Worker 时间戳逐字稿生成事实、判断、建议各一条，每条绑定真实时间证据。
- 教师复核历史、报告持久化与导出已有后端数据库测试和真实 API 验收；Markdown、HTML、
  PDF 均已写入对象存储、下载并验证内容门禁。
- 当前缺口集中在浏览器全程点击、第二输入、同任务复测、真实翻译/课件与正式部署，
  因此已有核心流程评分证据，但仍不具备最终满分证据。

## 小组报告（30 分）

- 需求与产品设计：10 分。
- 技术实现与实际完成范围：10 分。
- 验证复盘与 AI 协作：10 分。

当前风险：

- `reports/group-report.md` 已从纯骨架更新为过程稿，但仍需各成员补充本人实现和证据。
- 真实用户试用与同任务复测延期，不能编造反馈或验证结论。
- AI 协作与跨成员修复必须保留原作者和协作归属。

## 个人贡献（10 分）

每位成员说明负责内容、可核对成果和协作贡献。

当前要求：贡献文件只记录可由提交、PR、测试或人工记录核对的事实；成员 1
协作完成的跨模块修复不得改写成其他成员已独立完成，其他成员的原始实现也不得
归入成员 1 的独立成果。

## 提交前仍须清零的门禁

- [x] Worker 能处理浏览器上传到 B2 的真实视频（PR #20；单输入真实验收）。
- [x] 真实逐字稿、证据、Agent 结论、教师复核和报告形成单输入技术链路。
- [x] 教师复核状态、修改内容和历史写入后端（真实数据公开业务 API 验收通过；非开发教师浏览器试用另列）。
- [x] 报告可持久化，并只读取已接受或已修改内容；含真实结论的三格式导出已核验。
- [ ] 第二段不同视频通过网页全链路复测。
- [ ] 一次成功流程和两类常见失败恢复已有记录；同任务修改后复测仍缺。
- [ ] 一键启动、部署地址、源码说明和全量回归可由其他成员复现（本机发布门禁已通过；完整基础设施、浏览器和他人复现仍缺）。
