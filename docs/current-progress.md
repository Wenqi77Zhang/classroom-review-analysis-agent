# 当前进度与集成阻塞

> 更新日期：2026-08-10
> 已完成事实基线：`main@5b4a95b`（已包含 PR #34 真实证据工作台、PR #35 真实报告工作流
> 与 PR #36 报告会话恢复）。当前分支 `member-1/real-e2e-acceptance` 记录真实纵向验收。
> “已完成”必须同时有代码和自动化或人工记录；只有代码、缺真实环境验证的项目标为
> “局部通过”，不计入里程碑 M1 完成范围。

## 总体结论

项目已经完成一条真实视频从 B2 上传、后端核验、任务创建、Worker 下载与 ASR、
时间戳逐字稿写回 PostgreSQL、Agent 本地模型分析、教师复核到报告导出的技术纵向链路。PR #26 已补出
`Worker 交接 → Agent 领取/续租 → 分析编排 → 结论写回 → 任务完成` 的可运行边界，
并通过离线自动化；2026-08-10 又以真实 Worker 逐字稿驱动本地 Ollama `qwen3.5:4b`，
生成 3 条带时间证据的结论。教师复核历史和报告持久化已随 PR #22 合入，并在 PR #31 的
PostgreSQL CI 中以全仓 `190 passed, 5 skipped` 复验；PR #34 已将真实逐字稿、结论与
教师复核接入证据工作台，PR #35/#36 已接入服务端报告、导出与会话恢复。本轮通过
公开业务 API 验证接受、修改、驳回及 Markdown/HTML/PDF 三格式真实导出。
PR #27 已补齐 Worker 内部常驻轮询、可替换翻译阶段、课件解析、证据索引草稿、
两类专业 Skill 与专业证据校验器并通过自动化；这些能力尚未接入远程纵向链路，
真实翻译、课件证据、第二段远程输入、非开发教师试用与同任务修改后复测仍未完成。
因此可声明“单输入真实数据技术 E2E 已通过”，但里程碑 M1 **尚未全部通过**，不得扩大为
浏览器用户验收或完整 M1 已完成。

本分支完成文档同步后执行正式发布门禁：Python `267 passed, 11 skipped`、Ruff、前端测试、
类型检查和 Next.js 生产构建通过。首次失败仅因 Codex 沙箱无法写入 Windows 用户临时目录；
改用工作区临时目录后复验通过，详见 `../tests/manual/failure-and-retry-record.md`。

当前主链路：

```text
浏览器 → B2 预签名上传 → HEAD 核验 → 创建 uploaded 任务
   PR #19 已合并并人工验证
        ↓
Worker 限时下载 → 大小/ETag 校验 → FFmpeg → Whisper → PostgreSQL 逐字稿
   PR #20 已合并；一段获授权真实视频验收通过
        ↓
Agent 本地模型 → 3 条带时间证据结论 → 接受/修改/驳回 → 持久化报告 → 三格式导出
   2026-08-10 单输入真实数据技术 E2E 已通过；浏览器全程点击与非开发教师试用仍待完成
```

## 按模块记录

| 模块 | 已合并到 `main` | 进行中或尚未完成 |
|---|---|---|
| 成员 1：产品流程前端 | 同源 BFF 演示会话；课程/课堂创建；B2 上传；任务状态；Day 3 静态可用性；PR #34 真实证据工作台；PR #35/#36 报告与会话恢复；单输入真实数据技术 E2E 验收 | 浏览器全程点击、第二输入和非开发教师试用仍待验收 |
| 成员 2：证据工作台 | 双语结构、播放器/时间轴/证据卡联动、接受/修改/驳回交互；PR #34 后页面已接入真实逐字稿、结论、复核写入和历史恢复 | 真实课件画面联动、真实翻译和非开发用户试用仍待验收 |
| 成员 3：平台后端 | 认证、课程/课堂、S3 Provider、上传核验、任务领取/状态/重试、逐字稿、结论、教师复核/历史、报告保存读取、Markdown/HTML/PDF 导出与账号隔离 | 尚余完整部署和浏览器纵向链路验证 |
| 成员 4：媒体 Worker | 两段真实视频本地 FFmpeg + Whisper 验证；PR #20 接通一段 B2 真实视频到 PostgreSQL 逐字稿；PR #27 已合并常驻轮询/退避、可替换翻译阶段、PDF/PPTX 解析、证据索引草稿、两类专业 Skill 与专业证据门禁，合并后专项 `63 passed` | 第二段远程输入、真实翻译 Provider、课件/证据后端写回及远程后续阶段尚未串联 |
| 成员 5：Agent 与质量 | 结构化分析编排、证据范围校验、隐私路由和报告组合规则；PR #26 已合并 Worker→Agent 运行时；PR #27 的两类专业 Skill/证据门禁已接入并通过自动化；PR #31 的 JSONL Trace、前端 `trace_id`、Ollama 接入和统一启动代码已合并；真实 Worker 逐字稿已驱动本地模型并写回 3 条证据结论 | 专业 Skill 真实输入、浏览器完整 E2E 与发布复现仍待验证 |

2026-08-14 的成员 1 工作分支新增双语契约补救闭环：纯中文可复用原资产关闭双语；
英文或中英混合课堂可上传 UTF-8 SRT/VTT 中文译文，经过对象存储核验后与原视频资产创建
新任务，Worker 重新从视频生成 ASR 原文并按时间覆盖对齐译文。当前专项 Python
`67 passed, 1 skipped`，完整 Python `273 passed, 11 skipped`，前端测试、TypeScript 与
生产构建通过；尚未使用真实 B2 对象和
浏览器完整点击复验，合并及人工复验前不得写成已完成用户验收或自动翻译能力。

## 当前验收判断

| 里程碑 M1 验收项 | 状态 | 当前证据或缺口 |
|---|---|---|
| 真实视频上传并创建任务 | 已完成 | PR #19；浏览器 B2 上传与任务创建人工验证 |
| 两段不同真实视频产生不同时间戳逐字稿 | 局部通过 | 两段本地真实视频结果不同；PR #20 又证明一段浏览器上传对象可经 B2、Worker 写回合法时间戳逐字稿；仍缺第二段远程输入复测 |
| 两账号隔离 | 自动化通过 | PostgreSQL 后端测试覆盖跨账号资源访问；尚未执行部署网页双账号复测 |
| 接受、修改、驳回并保留历史 | 技术链路通过 | 真实 Agent 结论经教师公开业务 API 分别接受、修改、驳回，三个 `201` 与历史持久化已核对；仍缺非开发教师浏览器独立操作 |
| 报告只包含已接受或已修改内容 | 技术链路通过 | 报告精确纳入 accepted/modified，使用教师改写文本并排除模型原判断与 rejected；Markdown/HTML/PDF 均真实下载核验 |
| 完整成功流程 | 技术链路通过 | 一条获授权真实视频完成 B2、Worker、时间戳逐字稿、Agent、复核、报告与三格式导出；仍不等于浏览器全程用户验收 |
| 常见失败、恢复和同任务复测 | 局部通过 | 已记录旧契约安全隔离和对象不存在两类真实失败，并通过重新上传/新建任务恢复；同任务修改后复测仍缺 |
| 非开发成员真实试用 | 已延期 | `tests/manual/usability-record.md` 保持未填写，不得视为通过 |
| 一键启动与部署 | 局部通过 | PR #31 的 `start.ps1` / `start.sh` 已覆盖前端、后端、Worker 和 Agent，完成语法、静态安全、进程清理及缺配置 fail-closed 测试；PR #30 临时团队入口保持不变；尚未完成完整基础设施与浏览器环境复验 |

## 下一次更新规则

1. PR #20 的 Worker/B2 集成只证明已经记录的一段真实输入和对应阶段，不自动覆盖第二输入或后续 Agent 链路。
2. 后续集成至少补充：使用的提交号、测试命令、结果、真实输入来源与脱敏记录。
3. 单输入真实数据技术 E2E 通过仍不等于 M1 通过；还必须继续验收第二输入、专业 Skill、
   翻译/课件证据、同任务复测、浏览器完整流程和真实试用。
4. 进度变化时同步更新本文件、`acceptance-matrix.md`、`scorecard.md` 和报告中的完成范围。
