# 当前进度与集成阻塞

> 更新日期：2026-07-30
> 已完成事实基线：`main@02d8e41`（已包含 PR #22 的复核与报告持久化）。
> 当前待合并后端分支：PR #24（Trace、恢复与幂等）及其叠加的报告导出、写操作审计分支。
> 状态口径：只有已经合并到 `main` 且有代码、测试或人工记录支持的内容才标记为
> “已完成”。其他对话或分支正在实现的内容统一标记为“进行中”，在合并和复测前
> 不计入里程碑 M1 完成范围。

## 总体结论

项目已经完成浏览器真实 B2 上传、后端核验、任务创建、Worker 下载与 ASR、
时间戳逐字稿写回 PostgreSQL 的单输入链路，并已合入
`Worker 交接 → Agent 领取/续租 → 分析编排 → 结论写回 → 任务完成` 的运行边界。
该边界已通过自动化，但尚未使用真实模型和真实证据完成全链路验收；翻译、证据索引、
教师复核持久化和报告持久化已进入 `main`；Trace、服务端导出和写操作审计仍在成员 3 分支验证，
尚未形成与前端和真实模型的完整联调。里程碑 M1 当前
**尚未通过**，不得描述为真实端到端全链路已经完成。

当前主链路：

```text
浏览器 → B2 预签名上传 → HEAD 核验 → 创建 uploaded 任务
   PR #19 已合并并人工验证
        ↓
Worker 限时下载 → 大小/ETag 校验 → FFmpeg → Whisper → PostgreSQL 逐字稿
   PR #20 已合并；一段获授权真实视频验收通过
        ↓
翻译/课件/证据索引 → Agent 结论 → 教师复核历史 → 持久化报告
   Agent 自动接管已合并并通过离线自动化；
   真实模型、真实证据、复核历史与报告仍未形成完整集成链路
```

## 按模块记录

| 模块 | 已合并到 `main` | 进行中或尚未完成 |
|---|---|---|
| 成员 1：产品流程前端 | 同源 BFF 演示会话；课程/课堂创建；B2 预签名上传、PUT、HEAD 完成核验；任务创建与状态查询；Day 3 静态可用性修复；报告打印预览 | 证据仍使用明确标注的演示数据；复核仅为页面状态；报告草稿仅保存在浏览器会话中 |
| 成员 2：证据工作台 | 双语结构、播放器/时间轴/证据卡联动、接受/修改/驳回交互与状态重置 | 尚未连接真实逐字稿、结论读取、教师复核写入与历史接口 |
| 成员 3：平台后端 | 认证、课程/课堂、S3 Provider、上传核验、任务领取/状态/重试、逐字稿读取编辑与 Worker 写入、结论读取与 Agent 写入、账号隔离测试 | PR #22 待合并：教师复核历史与报告保存；后续堆叠分支已实现白名单 Trace/审计 API、幂等回归和应用重建后租约恢复，待提交；尚余报告导出、完整部署验证 |
| 成员 4：媒体 Worker | 两段真实视频本地 FFmpeg + Whisper 验证；PR #20 由成员 1 协作接通 `uploaded` 任务领取、B2 限时下载、大小/ETag 校验、真实视频 ASR 与 PostgreSQL 逐字稿写回 | 生产级常驻轮询与退避、第二段远程输入、翻译、课件解析、证据索引和后续阶段尚未串联 |
| 成员 5：Agent 与质量 | 结构化分析编排、证据范围校验、隐私路由、Trace 结构、只组合已接受/修改结论的确定性规则和自动化测试；成员 1 暂代集成的 Worker→Agent 原子交接、Agent 领取/续租/状态/结论写回和失败脱敏已合并 | 真实模型、真实证据索引、后端 Trace/报告持久化、完整 E2E 和一键启动仍未通过 |

## 当前验收判断

| 里程碑 M1 验收项 | 状态 | 当前证据或缺口 |
|---|---|---|
| 真实视频上传并创建任务 | 已完成 | PR #19；浏览器 B2 上传与任务创建人工验证 |
| 两段不同真实视频产生不同时间戳逐字稿 | 局部通过 | 两段本地真实视频结果不同；PR #20 又证明一段浏览器上传对象可经 B2、Worker 写回合法时间戳逐字稿；仍缺第二段远程输入复测 |
| 两账号隔离 | 自动化通过 | PostgreSQL 后端测试覆盖跨账号资源访问；尚未执行部署网页双账号复测 |
| 接受、修改、驳回并保留历史 | 进行中 | 后端真实 PostgreSQL HTTP 用例已在成员 3 分支通过，前端尚未接线且尚未合并 |
| 报告只包含已接受或已修改内容 | 进行中 | 后端持久化门禁已在成员 3 分支通过；Agent 组合器和前端演示测试通过，尚未合并联调 |
| 完整成功流程 | 未通过 | 上传到 Agent 接管与结论写回已有自动化边界；尚缺真实模型、真实证据、翻译、复核和报告联调 |
| 常见失败、恢复和同任务复测 | 未通过 | 有局部失败测试；没有完整用户流程记录 |
| 非开发成员真实试用 | 已延期 | `tests/manual/usability-record.md` 保持未填写，不得视为通过 |
| 一键启动与部署 | 未通过 | `start.ps1` 仍是占位入口，尚不能统一启动前端、后端和 Worker |

## 下一次更新规则

1. PR #20 的 Worker/B2 集成只证明已经记录的一段真实输入和对应阶段，不自动覆盖第二输入或后续 Agent 链路。
2. 后续集成至少补充：使用的提交号、测试命令、结果、真实输入来源与脱敏记录。
3. Worker 集成或 Agent 分支自动化通过均不等于 M1 通过；还必须继续验收真实模型与证据、教师复核历史、报告持久化、
   第二输入、失败恢复和真实试用。
4. 进度变化时同步更新本文件、`acceptance-matrix.md`、`scorecard.md` 和报告中的完成范围。
