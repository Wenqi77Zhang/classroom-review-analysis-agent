# 项目协作规则

1. `docs/project-plan-v5.md`、需求基线和验收矩阵是当前事实源；范围变化必须先经成员 1（组长）确认。
2. “M1/M2/M3”只表示里程碑；团队人员始终写成“成员 1”至“成员 5”。
3. 未实现内容必须标记 `TODO`；模拟内容不得描述为真实运行。
4. 每个改动应关联责任文件、测试证据和 AI 协作记录。
5. 不提交视频、密钥、账号、Cookie、个人信息、数据库转储或对象存储签名 URL。
6. Python 环境使用仓库根目录 `.venv`；不得依赖全局 Python 包。
7. 修改跨模块 Schema 前更新 `docs/interface-contracts.md`，并取得相关负责人确认。
8. `worker/` 只加工媒体与证据；`agent/` 只基于证据进行受约束分析。
9. 未复核或已驳回结论不得进入报告。
10. 合并前运行与改动相关的测试；重大问题不得绕过发布门禁。
