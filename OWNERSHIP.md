# 文件与模块责任

| 范围 | 第一负责人 | 协作者 | 核心任务 | 完成证据 |
|---|---|---|---|---|
| `docs/`、`frontend/src/components/baseline/`、流程页面、`reports/` | 成员 1（组长） | 成员 2、5 | 产品范围、UI Baseline v1、首页/课堂/复盘目标流程、用户试用、报告统稿 | 产品记录、界面截图、提交、试用记录、报告 |
| `frontend/src/components/evidence/`、证据工作台与前端工程维护 | 成员 2 | 成员 1、3 | 在 UI Baseline v1 上实现播放器、时间轴、逐字稿、证据、复核、响应式并维护依赖 | `package-lock.json`、前端测试与联动录像 |
| `backend/` | 成员 3 | 成员 2、4、5 | FastAPI、数据库、权限、Backblaze B2 的 S3 Provider、预签名 URL、对象地址、状态和审计 | OpenAPI、迁移、B2 上传/读取、Provider 与权限测试 |
| `worker/` | 成员 4 | 成员 3、5 | 经 Provider 读取 B2 对象、FFmpeg、ASR、翻译、课件、证据索引和重试 | 两段视频、B2 对象读写与失败重试记录 |
| `agent/` | 成员 5 | 成员 1、3、4 | 编排、模型、Skill、Schema、证据门禁、报告组合、Trace | Agent 测试与追踪记录 |
| `tests/`、CI、部署 | 成员 5 统筹 | 全员 | 自动化、E2E、真实试用、安全和发布门禁 | 测试记录、部署链接 |

## 前端基准变更约束

- 成员 1 对 `UI Baseline v1` 的产品叙事、视觉令牌和流程入口作最终确认。
- 成员 2 在此基准上实现证据工作台，可正常重构复用组件；涉及字体、字号下限、色板、背景或主流程的整体变化需在 PR 中附截图并请求成员 1 复核。
- 当前基准由成员 1 与 Codex 协作形成，不得写成成员 1 独立完成的全部技术实现。

每个模块的唯一说明文件必须补充目的、输入、输出、接口、依赖、测试和完成定义。更细的文件级责任见 `docs/project-plan-v5.md`。
