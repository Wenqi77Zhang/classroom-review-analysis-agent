# 课堂复盘与教学分析系统：小组报告

> 当前为 2026-07-30 过程稿，事实基线为 `main@f2349d6`。本文不代表
> 里程碑 M1 已完成；各成员仍需补充本人实现说明和脱敏证据。

## 1. 问题、背景与用户

系统面向需要复盘单节公开课的高校教师，目标是把课堂视频转化为可定位、可复核的
教学分析，而不是自动给教师打分。每条判断必须关联时间戳逐字稿、视频、课件页或
画面证据；只有教师接受或修改确认的内容才允许进入报告。

## 2. 产品定位、流程与交互

成员 1 冻结了从“说明复盘目标—确认分析契约—上传资料—查看处理状态—核验证据—
人工复核—编辑和导出报告”的 M1 流程与 UI Baseline v1。当前真实课程/课堂、
B2 上传和任务创建已经接入；证据、教师复核和报告仍保留明确的演示边界。

## 3. 前端架构与证据工作台

Next.js 前端通过同源 BFF 调用后端，短期 JWT 保存在 HttpOnly Cookie，浏览器不接触
服务令牌或 B2 长期密钥。成员 2 的播放器、时间轴、双语逐字稿和证据卡组件已经合并，
但真实逐字稿、结论、复核历史仍待 API 接线。

## 4. 平台后端、数据与安全

FastAPI 与 PostgreSQL 已实现账号隔离、课程/课堂、S3 Provider、上传核验、任务租约、
逐字稿和结论的最小权限读写边界。真实 B2 上传由浏览器完成，数据库只保存对象元数据。
教师复核历史和报告 API 尚未实现。成员 3 仍需核对并补充本人实现说明。

## 5. 视频、逐字稿、翻译与专业规则

成员 4 已用两段不同真实视频验证 FFmpeg 音频抽取和本地 Whisper 时间戳逐字稿。
PR #20 由成员 1 协作补齐 `uploaded` 任务领取、B2 限时下载、大小/ETag 校验和
PostgreSQL 写回，并用一段获授权真实视频完成
`B2 → Worker → FFmpeg → Whisper → transcript` 验收。媒体流水线主责仍归成员 4；
翻译、课件解析、证据索引、生产级常驻轮询及第二段远程输入尚未完成。

## 6. Agent 架构与可观测性

Agent 已实现结构化事实/判断/建议、任务与账号证据范围校验、隐私路由、稳定 Trace
结构和只组合 `accepted/modified` 的确定性规则。成员 1 暂代成员 5 的跨模块集成，
在当前尚未合并的分支补出 Worker→Agent 原子交接、Agent 领取与续租、编排、结论写回、
任务完成和安全失败处理；离线自动化已通过。Agent 主责仍属于成员 5，且真实模型、
真实证据输入、后端 Trace/报告持久化尚未完成验收。

## 7. 测试、真实试用与迭代

前端测试、类型检查和生产构建已通过当前合并门禁；后端和 Worker 有分层自动化与
PostgreSQL 隔离测试。Day 3 静态可用性审查和报告打印人工检查已完成。非开发成员
独立试用、常见失败恢复和同任务复测经成员 1 确认暂时延期，因此不能宣称 Day 3
退出条件通过。

## 8. 完成范围、限制和后续计划

已完成的主要纵向切片已推进为“浏览器真实上传到 B2—后端核验—创建任务—
Worker 下载与转写—PostgreSQL 时间戳逐字稿”。
里程碑 M1 尚缺第二段远程输入、翻译与真实证据索引、Agent 分支合并及真实模型验收、教师复核历史、
报告持久化、第二输入网页复测、失败恢复、真实试用和统一启动。详细矩阵见
`../docs/current-progress.md`。

## 9. AI 协作

Codex 参与需求整理、实现协作、代码审查、测试与文档同步；成员负责产品决定、
授权、人工核验和最终采用。AI 生成或协作修复的内容不冒充成员独立完成，详细记录见
`../docs/ai-collaboration-log.md`。

## 10. 个人贡献

个人贡献引用 `contributions/`。合并前由每位成员核对本人文件；跨成员修复分别记录
原始实现和后续协作，不转移作者归属。
