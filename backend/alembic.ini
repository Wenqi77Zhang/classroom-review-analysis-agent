[alembic]
script_location = backend/migrations
prepend_sys_path = .

# migrations/env.py reads the real URL from Settings/.env. Never store credentials here.
sqlalchemy.url = postgresql://unused:unused@localhost/unused
